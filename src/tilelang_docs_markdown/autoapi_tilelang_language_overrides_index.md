# tilelang.language.overridesÂ¶

TileLang-specific runtime overrides.

Importing this package registers custom handlers that extend or override behavior from upstream TVMScript for TileLang semantics.
